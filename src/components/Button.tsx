import React from "react";

/**
 * 1. Icon (On, Off)
 * 2. Color (Warning, Confirm, Default)
 * 3. Size (S, M, L)
 * 4. Opacity (Disable, Enable)
 */

const Button = () => {
  return null;
};

export default Button;
